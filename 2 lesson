1. Синтаксис объявления Функций
    - Документация функции
        def foo():
            '''I return 42'''
            return 42
    - После объявления функции документация доступна через атрибут
        foo.__doc__
    - В интерпретаторе удобнее пользоваться встроенной функцией
        help(foo) #или foo? в iPython
2. Функции
    2.1 min() #выводит минимальное число
        def min(x, y):
            return x if x < y else y
        min(-5, 10)
        -5
        min(x=10, y=5)
        5
        min(x=10, y=5, z=2) #Error, в функции min() нету аргумента z, но min(10, 5, 2) работает
    2.2